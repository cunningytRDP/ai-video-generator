services:
  - type: web
    name: ai-video-generator
    env: python
    buildCommand: pip install -r video-generator/requirements.txt
    startCommand: python video-generator.py
    plan: free
